# 环境变量配置模板
# 复制此文件为 .env.local 或其他环境文件并填写实际值

# API基础地址
# 开发环境建议使用 /api（会通过代理转发）
# 生产环境使用完整URL，如 http://your-server:8567/api
VITE_API_BASE_URL=

# 开发环境代理目标（仅在开发环境使用）
# 配置实际的后端服务地址
VITE_API_PROXY_TARGET=

# WebSocket地址（可选）
# 不配置时会自动从API地址推断
# 格式: ws://host:port 或 wss://host:port
VITE_WS_URL=

# 配置示例：
# 
# 本地开发连接本地后端：
# VITE_API_BASE_URL=/api
# VITE_API_PROXY_TARGET=http://localhost:8567
#
# 本地开发连接远程后端：
# VITE_API_BASE_URL=/api
# VITE_API_PROXY_TARGET=http://218.78.128.120:8567
#
# 生产环境：
# VITE_API_BASE_URL=http://218.78.128.120:8567/api